import { SocketDirective } from './socket.directive';

describe('SocketDirective', () => {
  it('should create an instance', () => {
    const directive = new SocketDirective();
    expect(directive).toBeTruthy();
  });
});
