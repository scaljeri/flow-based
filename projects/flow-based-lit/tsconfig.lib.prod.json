{
  "extends": "./tsconfig.lib.json",
  "compilerOptions": {
    "declarationMap": false
  },
  "angularCompilerOptions": {
    /*
     * "partial" is the publishable mode, and there is nothing here that needs
     * more: this package contains no Angular components at all, only Lit
     * elements over the framework-free core.
     */
    "compilationMode": "partial"
  }
}
